# Warranty Claims Agent - Environment Configuration
# Copy this file to .env and configure your settings

# ===========================================
# LLM Configuration (choose one or more)
# ===========================================

# Option 1: Ollama (LOCAL - Recommended for development)
# No API key needed, just run: ollama serve
USE_OLLAMA=true
OLLAMA_MODEL=qwen2.5:1.5b
OLLAMA_URL=http://localhost:11434

# Option 2: Groq (CLOUD - Fast inference, generous free tier)
# Get API key at: https://console.groq.com/keys
# GROQ_API_KEY=your_groq_api_key_here
# GROQ_MODEL=llama-3.3-70b-versatile

# Option 3: Google Gemini (CLOUD - Reliable fallback)
# Get API key at: https://makersuite.google.com/app/apikey
# GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_MODEL=gemini-2.0-flash

# ===========================================
# Priority Order
# ===========================================
# The system tries LLMs in this order:
# 1. Ollama (if USE_OLLAMA=true and Ollama is running)
# 2. Groq (if GROQ_API_KEY is set)
# 3. Gemini (if GOOGLE_API_KEY is set)

# ===========================================
# RAG Embeddings
# ===========================================
# Use hash to avoid external model downloads.
EMBEDDING_MODE=hash
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ===========================================
# Demo / Local Modes
# ===========================================
# When true, the workflow runs without any external LLM calls and uses
# deterministic heuristics for triage/extraction/analysis.
DEMO_MODE=false

# ===========================================
# Storage Locations (Optional Overrides)
# ===========================================
# By default, policies are loaded from data/policies and ChromaDB persists to outbox/chroma_db.
# Uncomment to override locations (recommended for real use to keep repo pristine).
# POLICIES_DIR=outbox/policies
# POLICY_INDEX_FILE=outbox/policies/index.json
# CHROMA_DIR=outbox/chroma_db

# ===========================================
# Outbound Email Delivery (Optional)
# ===========================================
# UI setting `email.outbound_mode` controls actual send path:
# - manual (default): do not send externally
# - gmail_api: send via connected Gmail account
# - smtp: send via SMTP settings below

# Shared sender identity (fallback)
# EMAIL_FROM=warranty@yourcompany.com

# SMTP settings (required if outbound mode is smtp)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_username
# SMTP_PASSWORD=your_password_or_app_password
# SMTP_USE_TLS=true
# SMTP_FROM=warranty@yourcompany.com
